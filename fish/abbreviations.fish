#
# Fish shell abbreviations are auto-expanded on space or tab, replacing the
# abbreviation with the specified text
#

abbr --add gc "git commit -am"
abbr --add gp "git push"
abbr --add @home "cd $HOME"
